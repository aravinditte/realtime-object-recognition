# Real-Time Object Detection Requirements
# Core dependencies for the application

# Web Framework
Flask==3.0.0
Flask-SocketIO==5.3.6

# Computer Vision and AI
# Use versions that provide prebuilt wheels for Windows and modern Python
ultralytics==8.3.34
opencv-python==4.9.0.80
numpy==1.26.4
Pillow==10.1.0
# Torch note: ultralytics will pull torch/torchvision; if install fails on Windows,
# install CPU wheels manually per https://pytorch.org/get-started/locally/
# Example (Windows CPU): pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# WebSocket and Async
eventlet==0.33.3
python-socketio==5.9.0

# Utilities
Werkzeug==3.0.1
requests==2.31.0
setuptools>=68
wheel>=0.41

# Development and Testing (optional)
pytest==7.4.3
black==23.10.1
flake8==6.1.0

# Production server (optional)
# gunicorn is not for Windows; kept for cross-platform
gunicorn==21.2.0
